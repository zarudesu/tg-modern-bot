# Dockerfile для простых миграций
FROM python:3.12-slim

WORKDIR /app

# Установим только нужные зависимости
RUN pip install asyncpg sqlalchemy alembic psycopg2-binary

# Копируем только миграции
COPY alembic/ alembic/
COPY alembic.ini .

CMD ["alembic", "upgrade", "head"]
